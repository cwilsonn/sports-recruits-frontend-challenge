# Sports Recruits Frontend Code Challenge

This is a code challenge for SportsRecruits.

## Development

- Run `yarn` to install dependencies
- Run `yarn serve` to serve the application locally
- Run `yarn test:unit` to run unit tests
